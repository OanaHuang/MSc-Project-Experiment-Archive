extends: ntu_cs80
training:
  epochs: 120
  milestone_epochs: [120]
  # Keep the 128-frame optimizer batch while using one high-throughput GPU step.
  batch_size: 128
  gradient_accumulation: 1
  mixed_precision: true
  checkpoint_interval: 5
