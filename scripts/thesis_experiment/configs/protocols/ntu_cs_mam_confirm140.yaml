extends: ntu_cs_confirm140
data:
  minimum_temporal_history: 15
training:
  clip_batch_size: 8
  num_workers: 4
  learning_rate: 0.0001
  gradient_clip: 1.0
  loss_all_frames: true
  progress_interval: 100
  scheduler:
    kind: warmup_cosine
    warmup_epochs: 5
    min_lr: 0.000001
